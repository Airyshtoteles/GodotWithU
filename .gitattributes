# Export filtering for Godot Asset Library submission
# Ensures release ZIPs contain only the plugin addon folder

# Exclude everything at root by default
* export-ignore

# Whitelist only essential files for the release
!.gitattributes export-ignore
!addons/ export-ignore
!LICENSE export-ignore
!README.md export-ignore
